import styled from "styled-components";

export const Button = styled.div`
    width: fit-content;
    display: flex;
    align-items: center;
    cursor: pointer;

    p {
        font-size: 0.9rem;
        color: #333;
    }
`;

export const Icon = styled.div`
    color: #333;
    margin-bottom: -5px;
    margin-right: 5px;
    font-size: 0.9rem;
`